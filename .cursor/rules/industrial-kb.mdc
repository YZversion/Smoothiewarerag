---
description: industrial-cpp-kb-lab 脚本规范 — PLAN 约束 + Ponytail 极简纪律（仅 lab/src）
globs: industrial-cpp-kb-lab/src/**
alwaysApply: false
---

> 进度与约束见根目录 [`AGENTS.md`](../../AGENTS.md)。本文件仅管写 `src/*.py` 时的纪律。

# industrial-cpp-kb-lab — 写代码前读 PLAN.md

目标：模块名 / 函数名 / G-code / error·stop 关键词 → **相关源码 + 解释 + 文件:行号引用**。练手 Smoothieware，未来复用到 wire bonder C++。

## 决策梯（写任何代码前先停在这里）

1. **要不要做？** 当前 phase 验收没过，不加下一层。
2. **lab 里有没有？** 复用现有脚本的路径与 CLI 模式，不另起炉灶。
3. **stdlib + 已有工具够不够？** `pathlib`、`json`、`subprocess`；允许 `rank_bm25`、`openai`、`rich`。
4. **验收缺口驱动增量**：eval 不够再加一项（hints / bundle / 权重），每次只加一项并复跑 eval。
5. **能迁移吗？** 换 wire bonder 代码库仍成立的规则才合并；禁止把 `eval_questions.json` 里的文件名硬编码进检索器。

## 硬边界（不谈判）

- **禁止**：Milvus / Qdrant / LangChain / LlamaIndex / Agent / 知识图谱进主流程；改 `repos/**`；ARM 构建。
- **产物只写**：`data/`、`index/`、`prompts/`、`notes/`、`eval/`；脚本只写 `src/`。
- **CodeGraph** 仅 Plan B，不接入 `app.py`。
- 换公司代码：`--repo-root` / `--src-root`，不物理替换目录。

## 脚本约定

```python
LAB_ROOT = Path(__file__).parent.parent
```

编号管道：`01_` 扫描 → `02_` 符号 → `03_build_chunks` + `03_search` → `04_answer` → `app.py`。

## 检索增量纪律

- `QUERY_HINTS`：**按意图触发**，不按关键词出现触发。
- 符号加权：优先泛化规则（`on_*` 频率、hint 一致性），禁止 per-question 文件名 `+5`。
- `trim_context_hits`：primary 先于 overview/header。
- bundle / 权重：**Recall@5 < 4/5 时再加**，一次一项。

## Phase 4–5

- Prompt → `prompts/code_qa.md`；逻辑 → `04_answer.py`。
- `LLM_PROVIDER` / `LLM_MODEL` / `LLM_API_KEY` 环境变量。
- `app.py`：`--json` 不用 Rich；默认 `top_k=8`。

## 完成前自检

```powershell
cd industrial-cpp-kb-lab
python src/03_search.py --eval
python src/run_regression.py --skip-llm
```

未过当前 phase 验收，不进入下一 phase 功能。
